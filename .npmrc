# Electron version configuration for Prism Editor build
# This file is required for the VS Code build system to determine the correct Electron version
ELECTRON_CUSTOM_VERSION=29.3.0

# Optional: If you need to specify a custom Electron mirror (useful for CI builds)
# ELECTRON_CUSTOM_URL_PREFIX=https://github.com/electron/electron/releases/download/v

# Optional: Disable signature verification if needed for your build environment
# ELECTRON_SKIP_BINARY_DOWNLOAD=1

# Optional: If you're using a custom build of Electron
# ELECTRON_CUSTOM_URL=https://github.com/electron/electron/releases/download/v29.3.0/electron-v29.3.0-linux-x64.zip
